c++で作ったライブラリをドキュメント化してwebで表示させる。
github pagesにデプロイするとweb上で見られるようになる。一般公開されるけど、しゃーない
doxygenを使うと自動でindex.htmlファイルを作ってくれるからそれをgithub actionを使ってデプロイする。
doxygenファイルを実行して作成されるフォルダをリポジトリに入れるとごちゃごちゃするから、action上で実行してリポジトリに生成フォルダ入れない
デプロイのタイミングはライブラリが更新された時やmainブランチにマージされた時